
class:
  name: Source
  superClass: RootEntity
  doc: A source is a literature reference.

  properties:

    - name: externalFile
      type: string
      doc: A direct link (relative or absolute URL) to the source file.

    - name: textReference
      type: string
      doc: The full text reference of the source.

    - name: url
      type: string
      doc: A URL that points to the source.

    - name: year
      type: integer
      doc: The publication year of the source.
