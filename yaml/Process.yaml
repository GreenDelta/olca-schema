
class:
  name: Process
  superClass: CategorizedEntity
  example: process.jsonld
  doc:

  properties:
    - name: defaultAllocationMethod
      type: AllocationType
      doc:

    - name: allocationFactors
      type: List[AllocationFactor]
      doc:

    - name: exchanges
      type: List[Exchange]
      doc: The inputs and outputs of the process.

    - name: location
      type: Location
      doc:

    - name: parameters
      type: List[Parameter]
      doc:

    - name: processDocumentation
      type: ProcessDocumentation
      doc:

    - name: processType
      type: ProcessType
      doc: